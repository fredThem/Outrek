<nav class="navbar navbar-expand-lg">
  <div class="container">
    <%= link_to :root do %>
    <%= show_svg('brand/Outrek_logoType.svg') %>
    <% end %>

      <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to 'Plan a trip', new_trip_path, class: 'btn btn-outline-primary my-2 my-sm-0 ml-2' %>
        </li>
        <li class="nav-item dropdown">
        <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
  </div>
</nav>
