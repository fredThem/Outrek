<div class="container">
  <h1>Invite a Friend to Your Trip</h1>
  <hr>
  <%# Select from list of friends? List of trips? %>

  <%= simple_form_for [ @trip, @invitation ] do |f| %>
    <%= f.input :email %>

    <%= f.button :submit, class:"btn btn-primary btn-block" %>
  <% end %>
</div>
